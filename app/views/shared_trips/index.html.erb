<h1>sharedtrips#index</h1>
<p>Find me in app/views/shared trips </p>

Shared Trip Dashboard



<!-- <h1>Copilot</h1> -->


<h3> List of all users added to this trip </h3>
<% @users.each do |user| %>

<p><%= user.username %></p>
<hr>
<% end %>


<h3> List of all shared trips </h3>


<% @shared_trips.each do |shared_trip| %>

<h3> trip id: <%= shared_trip.trip_id %></h3>
<h3> user  id: <%= shared_trip.user_id %></h3>
<h3> shared id: <%= shared_trip.shared_id %></h3>

<h3> User who owns that shared id: 
<% if @users.find(shared_trip.shared_id) %>
<%= found it %>

<% else %>
<%= Could nt find user with that id %>

<% end %>
</h3>
<hr>
<% end %>










<h2>ignore this:  List of all trips </h2>
<%= link_to "Send an invitation to a friend to share this trip!", new_trip_shared_trip_path %>
<hr>
<% @trips.each do |trip| %>
<%= link_to image_tag(trip.trip_image_url), trip_path(trip) %>
<h2><%= link_to trip.title %></h2>
<h3><%= trip.start_time %></h3>
<p><%= trip.description %></p>
<hr>
<% end %>






